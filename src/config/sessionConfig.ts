// src/config/sessionConfig.ts

export const EXTENDED_SESSION_CONFIG = {
  PERIODIC_CHECK_INTERVAL: 10 * 60 * 1000, // 10 minutes
  VALIDATION_BEFORE_REQUEST_INTERVAL: 5 * 60 * 1000, // 5 minutes
  
  AUTH_REQUEST_TIMEOUT: 30000,
  CHAT_REQUEST_TIMEOUT: 120000,
  STATUS_REQUEST_TIMEOUT: 15000,
  
  BIOMETRIC_RECOVERY_ENABLED: true,
  SILENT_RECOVERY_ATTEMPTS: 1,
  
  CLIENT_SESSION_DURATION: 12 * 60 * 60 * 1000, // ← 12 hours instead of 8
  REFRESH_TOKEN_BEFORE_EXPIRY: 10 * 60 * 1000,
  WARN_BEFORE_EXPIRY: 30 * 60 * 1000,
  
  MAX_SESSION_VALIDATION_RETRIES: 3,
  RETRY_DELAY_MS: 5000,
  
  USER_ACTIVITY_TIMEOUT: 8 * 60 * 60 * 1000, // ← 8 hours instead of 4
  ACTIVITY_CHECK_INTERVAL: 10 * 60 * 1000,
};